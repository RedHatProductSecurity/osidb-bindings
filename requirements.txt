anyio==3.3.1
attrs==21.2.0
certifi==2021.5.30
charset-normalizer==2.0.6
h11==0.12.0
httpcore==0.13.7
httpx==0.19.0
idna==3.2
python-dateutil==2.8.2
rfc3986==1.5.0
six==1.16.0
sniffio==1.2.0
# TODO: Copied just because I don't have Poetry set up yet
# We can use Poetry if we want, or switch to requirements.txt if not
